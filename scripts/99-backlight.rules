# Make the intel backlight sysfs file group-writable so non-root users
# (e.g. the hiluxOS backend) can adjust brightness.
ACTION=="add", SUBSYSTEM=="backlight", KERNEL=="intel_backlight", GROUP="video", MODE="0664"
