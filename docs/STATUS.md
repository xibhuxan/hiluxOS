# Estado del proyecto — hiluxOS

Última actualización: 2026-09-08 (**Ecualizador / Audio DSP** — pantalla `/equalizer` con EQ gráfico de 8 bandas, curva de respuesta en vivo, presets (built-in + usuario), balance estéreo y loudness; backend HAL con driver PipeWire filter-chain + mock y persistencia; 351 tests totales)

## Stack y ramas

- **Stack**: NestJS 11 + Prisma 6 + PostgreSQL 17 (backend, host) · Flutter stable (app, host) · PostgreSQL en Docker.
- **Versión**: `0.1.0` (`VERSION.txt`).
- **Ramas** (todas sincronizadas con `origin`; hash = último commit de código, los docs van detrás):
  - `master` → `2c417f7` — rama de release.
  - `dev` → `2c417f7` — integración.
  - `feature/develop` (activa) → `1c347bf` — desarrollo en curso (Vehículo UI v2: pestaña Coche + fixes).
- Working tree limpio. Todo publicado en el remoto.
- Recordatorios del entorno: Flutter en `/home/xibhu/flutter/bin/flutter` y Docker/postgres/conexiones a localhost se ejecutan **con sandbox desactivado**.

## Estado de compilación

- Backend: `tsc --noEmit` → **0 errores**.
- Flutter: `flutter analyze` → **0 issues** (lints `unnecessary_underscores` corregidos en Fase 0).
- **Tests**:
  - Backend unitarios (Jest): **149 tests** — `health.service`, `settings.service`, `tasks.service`, `radio.service`, `system.service`, `notifications.service`, `event-log.service`, `media-library.service` (mock Prisma + `CommandRunner` fake; `fs` mockeado para brightness y media scan; **multi-carpeta** vía `MediaFoldersService.scanRoots()`; scan no-op con tabla vacía), `media-art.service` (portadas: cover de carpeta, extracción ffmpeg con cache + marcador negativo), **`media-folders.service`** (seed MEDIA_DIR one-shot con flag `media.folders.seeded` — no re-siembra tras borrar todo, add con validación de absoluta/raíz/duplicado, remove con purga de tracks, exists flag, `scanRoots()` devuelve `[]` con tabla vacía sin throw), **`vehicle.service`** (MockVehicleDriver determinista con reloj inyectable: telemetría por funciones del tiempo sin timers — bounds, calentamiento exponencial, evolución entre polls, luces parciales, exclusión mutua hazard/intermitentes, cierre central, **puertas por id, alarma, ignición con acumulador de tiempo de motor (apagar congela odómetro/combustible, decae refrigerante, batería a reposo; re-encender reanuda; toggles idempotentes sin drift)**, animación analítica de ventanillas con parada y reanudación), **`power.service`** (MockPowerDriver sano; RpiPowerDriver parsea `vcgencmd get_throttled` — bitmask bits 0-2 actuales y 16-18 sticky, degrada a `available:false` sin vcgencmd), **`gpio.service`** (MockGpioDriver: pinout por defecto, escritura output, throw en pin desconocido y en pin input), **`equalizer.service`** (EQ de 8 bandas: clamp de ganancias ±12 dB y balance ±1, persistencia en `settings` best-effort, setBand marca curva custom, reset plano, presets built-in + usuario con shadow/delete y protección de built-ins, restore en `onModuleInit`, supervivencia a DB caída).
  - Backend e2e (Supertest): **101 tests** — `health`, `tasks`, `settings`, `radio`, `system`, `notifications`, `event-log`, `media` controllers con AppModule completa, mock Prisma + EventsGateway + fetch. El de media incluye art con ffmpeg real (MEDIA_DIR de usar-y-tirar en tmp) y **CRUD de carpetas** (GET/POST/DELETE con exists flag y purga de tracks; **sin resucitar `MEDIA_DIR` con el flag de seed puesto**). **`vehicle.e2e`** (GET con el módulo real validando la factory DI; acciones PUT/POST con driver mockeado vía token `VEHICLE_DRIVER`, whitelist 400, 404 en ventanilla desconocida, **ignición apaga también las largas, puertas con 404 en id desconocido, alarma**), **`power.e2e`** (GET con wiring real; sustitución por token; `SystemMonitorService.runOnce()` envía notificación de subtensión y calla cuando `available:false`), **`gpio.e2e`** (pinout, escritura, 404 pin desconocido, 400 pin input y valor no booleano).
  - Flutter: **101 tests** — `splash_screen`, `home_screen`, `quick_panel`, `pendientes_card`, `network_settings`, `radio_stop_resume` (+sentinel), `visualizer_style` (+sentinel), `media_provider` (+cola/shuffle, +409 duplicado → mensaje claro), `media_screen` (+árbol de carpetas, +chevrons de paneles, +selector vista álbum/espectro, **+raíl de carpetas configuradas con aviso de missing y confirm de borrado, +selector de carpeta del sistema**), `shell_title`, **`fullscreen_host`** (overlay tap-to-exit cubre TODO el shell incl. panel de estado; exit por tap en cualquier punto), **`radio_polish`** (shimmer en búsqueda, now-playing strip con badge EN DIRECTO, highlight de emisora actual, pop del favorito), **`system_polish`** (SectionHeader en las 3 cards, Reintentar en español, **celda Energía**), **`system_widget`** (celda Versión con hint de actualización), **`vehicle_provider`** (parseo del snapshot completo y degradado, load/setLock contra FakeAdapter, retención del snapshot en error, **+ignition/doors/alarm/signals contra FakeAdapter**), **`vehicle_screen`** (modo Control con las 6 tarjetas, modo Dashboard con gauges/métricas/testigos, botones one-shot de intermitentes, **+toggle de 3 modos ida y vuelta, +interpolación en vivo de la barra de ventanilla, +pestaña Coche por defecto con diagrama, +tap en puerta/vidrio del diagrama**), **`vehicle_card`** (telemetría + chips conectado, degradación "No conectado", estado backend caído), **`equalizer_screen`** (render de 8 bandas y presets, tap de preset lo aplica, switch de enable, botón reset, estado "Audio no disponible"), `widget_test` (providers mockeados con fakes que evitan red/timers).
  - **Total: 351 tests** (149 unit + 101 e2e + 101 Flutter). Comando e2e: `npm run test:e2e`.

## Qué funciona (verificado E2E en Linux desktop)

### Backend (`backend/`)
- `GET /api/health` — liveness + BD.
- `GET /api/system/info`, `/api/system/resources` (CPU/RAM/temp/**disco**/uptime/load).
- `GET|PUT /api/system/audio` — volumen real del SO (wpctl, amixer fallback) + mute.
- `GET|PUT /api/system/network` — WiFi real (nmcli) + toggle.
- `GET /api/system/network/wifi/scan`, `POST .../wifi/connect` (ssid+password), `POST .../wifi/disconnect`, `POST .../wifi/forget` — escaneo, conexión (con contraseña), desconexión y olvido de redes WiFi.
- `GET|PUT /api/system/bluetooth` — Bluetooth real (bluetoothctl) + toggle.
- `GET /api/system/network/bluetooth/scan`, `POST .../bluetooth/pair` (mac+pin opcional), `POST .../bluetooth/connect`, `POST .../bluetooth/disconnect`, `POST .../bluetooth/remove` — escaneo, emparejamiento (con PIN vía stdin), conexión, desconexión y olvido de dispositivos BT. `SystemService` refactorizado con `CommandRunner` inyectable (mockeable en tests).
- `GET|PUT /api/system/brightness` — brillo real vía sysfs (lectura/escritura en `/sys/class/backlight/intel_backlight/brightness`).
- `GET|PUT|DELETE /api/settings` — CRUD de ajustes.
- `GET /api/radio/stations/search`, `/radio/favorites` (GET/POST/DELETE), `/radio/history`, `/radio/stream/:id` — Radio Browser API. **Fix 2026-09-01**: favoritos e historial persistían mal por 3 bugs encadenados — (1) `Station.toJson()` enviaba `id` → 400 `forbidNonWhitelisted`; (2)+(3) `upsert` Prisma 6.x con `update: {}` vacío enrutaba a create → 500 unique constraint. Los upserts ahora llevan `update` no vacío (lección aplicable a todo Prisma 6.x del proyecto).
- `GET /api/tasks` (+ POST/PUT/DELETE) — módulo Pendientes, con seed (ITV, aceite, update, backup).
- `GET /api/notifications` — sistema de notificaciones (creación, listado, marcar leída = borrar).
- `GET /api/event-log` — registro de eventos del sistema.
- **HAL Fase 3**: `GET /api/vehicle` (telemetría: RPM/velocidad/refrigerante/combustible/odómetro/batería + luces, intermitentes, cierre, **alarma**, 4 ventanillas y 4 puertas) y acciones `PUT /api/vehicle/lights|signals|lock|ignition|alarm`, `PUT /api/vehicle/doors/:id`, `POST /api/vehicle/windows/:id/up|down|stop`; `GET /api/power` (salud energética de la Pi); `GET /api/gpio`, `PUT /api/gpio/:id`. Drivers sustituibles por env (`HAL_VEHICLE`, `HAL_POWER`, `HAL_GPIO`), defaults mock; verificado en vivo contra el dev server (luces persisten, ventanilla anima 0→1 en tiempo real; apagar motor → RPM/velocidad 0, batería a reposo ~12.4 V, odómetro congelado y refrigerante decayendo; re-encender reanuda).
- WebSocket `/events` (gateway, `ws` en lugar de Socket.IO para compatibilidad con Flutter).
- **Sistema OTA** (`/api/updates`): deploy blue-green, rollback automático, comprobación por `VERSION.txt`, descarga del tarball master, y **actualización del bundle UI** desde GitHub Releases + reinicio de cage. Endpoints de estado/verificación/aplicación.
- Migraciones Prisma aplicadas; seed ejecutado.

### Flutter (`app/`)
- Splash animado (logo +50%, glow pulsante, barra animada, transición).
- Radio: búsqueda Radio Browser, favoritos, historial, playback (`audioplayers`) y **visualizador de espectro** con ring buffer + 15 estilos (commit 9644ac3).
- Shell: panel superior fijo opaco con **volumen a la izquierda** (slider 180px táctil, funcional), reloj, Home, Apps; cajón de apps (end drawer) con tiles.
- **Quick Panel**: overlay deslizante desde el panel superior con toggles WiFi/BT, sliders volumen/brillo, indicadores de Internet y Backend. Cierra tocando fuera.
- Home: barra contextual + 4 cards (Estado actual, Sistema, Vehículo, Pendientes) en grid 2×2 sin scroll.
- Pantallas: Radio (búsqueda, favoritos, historial, playback + visualizador), System, Settings — cableadas al backend.
- **App Vehículo** ✅ 2026-09-06, **v2 2026-09-08**: pantalla `/vehicle` (ruteada, tile "Vehículo" en el cajón — sustituye al tile `/obd` deshabilitado según ADR-0002 — y título "Vehículo" en la barra superior) con **tres pestañas** que comparten el mismo `vehicleProvider` (un solo poll cada 3 s): **Coche** — vista cenital interactiva del Hilux pintada con `CustomPainter` (carrocería con capó/habitáculo/caja, 4 puertas que se dibujan **batidas** en warning al abrir, 4 ventanillas con el vidrio bajando según posición en vivo, faros por tipo — posición/cortas/largas/antiniebla/auxiliares — e intermitentes parpadeando a ~1.4 Hz en las esquinas); tocar una puerta la abre/cierra, tocar el vidrio cicla subir/parar/bajar. **Control** — botón de arranque tipo START/STOP (glow verde encendido), tarjeta "Luces" (posición/cortas/largas/antiniebla/auxiliares; encender largas arrastra cortas, como en un coche real), "Intermitentes" (izq/warning/dcha one-shot con exclusión mutua del mock), "Cierre y alarma" (cierre central + alarma antirrobo), "Ventanillas" (por ventanilla: barra de posición **interpolada en vivo** + ▲ ■ ▼) y "Puertas" (abrir/cerrar por puerta). **Dashboard** — velocímetro (0–160) y tacómetro (0–4000, zona roja desde 3200) pintados con `CustomPainter` (arcos, ticks, números, aguja) con interpolación suave entre polls (`AnimatedGauge`), **número del valor bajo el pivote de la aguja** (como un cuadro real), columna de métricas combustible/refrigerante/batería/odómetro con `AnimatedProgressBar` y colores por umbral, y **testigos del cuadro** (intermitentes parpadeando, luces, alarma, cierre, puerta abierta). Botón ⛶ que reutiliza el `FullscreenHost` del shell (dashboard a pantalla completa, tap para salir). Estados degradación: "No conectado" y "Backend no disponible", igual que la card del Home. **Fixes v2**: el toggle de modos emite el valor de cada segmento (antes el segmento inactivo re-emitía el modo actual y desde Dashboard no se podía volver a Control); las ventanillas interpolan su progreso localmente con un `Ticker` (helper compartido `WindowPositionLerp`, misma velocidad 0.5/s que el mock) en vez de saltar a cada poll.
- **Modo pantalla completa táctil** ✅ 2026-09-05: `FullscreenHost` — overlay en el `Stack` raíz del `AppShell` (FUERA del Scaffold, cubre TODO incl. el panel de estado; pantalla negra). Botón ⛶ ("Pantalla completa") en la fila de controles de Media y Radio. **Tap en cualquier punto = salir.** Media: modo álbum → portada con efecto **disco de vinilo** (rotación lenta 12s mientras suena, surcos pintados con CustomPainter, orificio central, la portada como etiqueta del disco); modo espectro → `SpectrumVisualizer` full-bleed (mismo widget compartido). Radio: espectro full-bleed. El contenido usa `Consumer`+`watch` para seguir vivo (cambio de pista/pausa) aunque el shell no observe el provider. v1 sin controles dentro, como se aprobó.
- **Settings → Wi-Fi & Bluetooth**: secciones dedicadas en la pantalla de Ajustes con toggle de radio, escaneo, lista de redes/dispositivos, diálogo de contraseña WiFi y diálogo de PIN Bluetooth.
- **Ecualizador / Audio DSP** ✅ 2026-09-08: pantalla `/equalizer` (ruteada, tile "Ecualizador" en el cajón, título en la barra superior). EQ gráfico de **8 bandas** (60/120/250/500/1k/2k/4k/8k Hz, ±12 dB) con **curva de respuesta suavizada pintada en vivo** detrás de los sliders (`_ResponseCurvePainter`, Catmull-Rom → Bézier con relleno degradado), chips de **presets** (Plano/Rock/Pop/Bass Boost/Vocal/Graves coche + los que guarde el usuario, con diálogo de guardado y protección de built-ins), slider de **balance estéreo** y toggle **Loudness**. Backend HAL (`equalizer` module) con driver **PipeWire filter-chain** (biquads `bq_peaking` vía `pw-cli`, sink por defecto) + **mock**; persistencia del estado y presets en `settings`; arrastre de sliders con throttle leader+trailing-queue (igual que el volumen). Degrada a "Audio no disponible" sin servidor de audio.
- **Teclado en pantalla** (`virtual_keypad`): teclado virtual pure-Dart integrado en el `AppShell` (modo standalone, se oculta sin foco). Cualquier `TextField`/`TextFormField` del app (incluido `TaskDialog`, contraseña WiFi, PIN BT) obtiene teclado táctil automáticamente — necesario porque la RPi+Cage/Wayland no tiene IME del sistema. `initializeKeyboardLayouts()` en `main()`.
- **Notificaciones**: panel + toast, provider conectado al backend.
- **Updates**: sección de actualización OTA en la UI.
- `flutter analyze` sin errores.

### Infra / despliegue
- `scripts/install-pi.sh` — instalador para Raspberry Pi OS (Debian-minimal): PostgreSQL auto-start, detección de node, build, prune de devDeps, salida a `/var/log/hiluxos-install.log`.
- `scripts/release-ui.sh` — publica el bundle Flutter como GitHub Release para que el OTA lo descargue.
- Cage kiosk mode (sin decoraciones de ventana), backend `seatd` habilitado para input devices.
- Despliegue a Pi no está totalmente automatizado a un solo comando todavía.

## Pendiente / siguientes pasos

### Fase 1 — Protección (tests) ✅
- **Tests unitarios backend (Jest)**: `health`, `settings`, `tasks`, `radio`, `system` (58 tests, mock Prisma + `CommandRunner` fake para nmcli/bluetoothctl; `fs` mockeado para brightness). ✅
- **Tests e2e backend (Supertest)**: `health`, `tasks`, `settings`, `radio`, `system` controllers (41 tests, AppModule completa con Prisma + EventsGateway + SystemService mockeados). ✅
- **Widget tests Flutter**: `splash`, `home`, `quick_panel`, `pendientes_card`, `network_settings` (22 tests, providers mockeados con fakes que evitan red/timers). ✅
- **Total: 121 tests** (58 unit + 41 e2e + 22 widget).
- **Siguiente**: ampliar cobertura — más pantallas Flutter (radio, system), controllers restantes (notifications, event-log, updates).

### Fase 2 — Cerrar lo casi-terminado
- ~~**Permisos de brightness**: automatizar el paso de `chmod`/`chown` (regla udev aplicable sin paso manual).~~ ✅ — regla udev genérica (`SUBSYSTEM=="backlight"`, sin filtrar por `KERNEL`) que cubre `intel_backlight` y `rpi_backlight`; `setup.sh` ahora dispara `udevadm trigger` tras instalarla para que se aplique sin reiniciar y muestra los permisos resultantes. Backend refactorizado: `backlightDir` auto-detecta el primer `/sys/class/backlight/*` (no más hardcodeo de `intel_backlight`), cacheado. 4 tests nuevos cubren lectura, escritura con path dinámico y caso sin backlight.
- ~~**Pantalla de configuración WiFi/Bluetooth**: los providers ya existen y funcionan, hoy solo informativos en la card Sistema. Conectarlos a una pantalla aparte.~~ ✅ — secciones `WifiSection` y `BluetoothSection` dentro de Settings: toggle, escaneo, lista de redes/dispositivos, diálogo de contraseña WiFi (`WifiPasswordDialog`) y diálogo de PIN BT (`BtPinDialog`). Backend con scan/connect/disconnect/forget/pair/remove vía nmcli/bluetoothctl. Teclado en pantalla `virtual_keypad` para meter texto en la RPi táctil.
- ~~**CRUD de Pendientes desde la UI**: el backend ya soporta POST/PUT/DELETE; la UI solo lista/completa. Rellenar crear/editar.~~ ✅ — botón `+` para crear, tap en título para editar, icono papelera para borrar (con confirmación). `TasksService` ahora lanza `NotFoundException` (404) en Prisma `P2025` en vez de propagar 500.
- ~~Ordenar pendientes por prioridad (el backend tiene `priority`, no se usa en UI).~~ ✅ — el backend ya ordena por `priority desc` en `findAll()`; la UI ahora muestra la prioridad y permite editarla con un slider 0–5 en el diálogo.

### Fase 3 — Diferenciador (HAL)
- ~~**HAL mock-first**: módulos `Vehicle`/`Power`/`GPIO` con interfaz + implementación mock (toggle por env)~~ ✅ 2026-09-06 — tres módulos HAL con arquitectura de sustitución (`ARCHITECTURE.md`): abstract driver + mock + factory DI seleccionada por env (`HAL_VEHICLE=mock|esp32`, `HAL_POWER=mock|rpi`, `HAL_GPIO=mock`; defaults mock, documentados en `.env.example`). **Vehicle**: `GET /api/vehicle` (telemetría completa), `PUT /lights|signals|lock`, `POST /windows/:id/up|down|stop` — el `MockVehicleDriver` genera telemetría como función pura del tiempo (senos + integral cerrada del odómetro; sin timers, reloj inyectable → determinista y testable), acciones mutan estado en memoria (hazard anula intermitentes, ventanillas animan analíticamente y congelan al llegar al tope); `ESP32VehicleDriver` es el seam real de ADR-0002 (protocolo TBD, reporta `connected:false`); card "Vehículo" del Home viva: velocidad grande + RPM, línea refrigerante/batería/combustible, chips (cortas, warning, cierre, puertas, ventanillas) y degradación a "No conectado" (snapshot desconectado o backend caído). **Power**: `GET /api/power` — `MockPowerDriver` (sano) y `RpiPowerDriver` (`vcgencmd get_throttled` vía `CommandRunner`, bits 0-2 actuales + 16-18 sticky, `available:false` en hosts sin vcgencmd); `SystemMonitorService.checkPower()` alerta "Subtensión en la Pi"/"CPU limitado (throttle)" con cooldown; celda "Energía" en la card Resources de la pantalla Sistema. **GPIO**: `GET /api/gpio`, `PUT /api/gpio/:id {value}` con `MockGpioDriver` (pinout BCM por defecto: backlight, botón, LED estado); 404 pin desconocido, 400 pin input. Driver GPIO real (Pi 5, `/dev/gpiomem`) fuera de scope: la actuación del vehículo vive en el ESP32 (ADR-0002) y el HAL queda listo para periféricos locales.
- ~~**Salud energética de la Pi**: undervoltage/throttle leyendo `/sys` o `vcgencmd`.~~ ✅ 2026-09-06 — parte del módulo Power (arriba): endpoint + alertas del monitor + celda en UI, de punta a punta.
- ~~**App Vehículo (actuación + dashboard)**: luces, intermitentes, puertas, alarma, ventanillas y arranque desde la UI; panel de visualización bonito.~~ ✅ 2026-09-06 — HAL ampliado primero: `setIgnition`/`setDoor`/`setAlarm` en el driver (mock con acumulador de tiempo de motor: apagar congela odómetro/combustible, decae refrigerante exponencialmente y baja la batería a reposo ~12.4 V; re-encender reanuda; el ESP32 los tiene como no-op hasta ADR-0002), endpoints `PUT /vehicle/ignition|alarm|doors/:id` (apagar el motor también baja las largas, como en un coche real), y `alarm.armed` en el snapshot. Luego la app `/vehicle` **de dos modos que comparten un solo poll** (arriba, en "Qué funciona — Flutter"): Control (todo actuabile) y Dashboard (gauges CustomPainter + métricas + testigos + pantalla completa con `FullscreenHost`). Tile "Vehículo" del cajón (sustituye al `/obd` deshabilitado, según ADR-0002). 16 tests nuevos (5 driver, 4 e2e, 7 Flutter — provider/screen). Verificado en vivo contra el dev server con curl y con la app Linux release ejecutándose. **v2 2026-09-08** (feedback real sobre la v1, ver `docs/NOTES-VEHICLE-UI.md`): tres bugs corregidos + tercera pestaña. Bug 1 — el toggle Control⇄Dashboard emitía `!active`, así que desde Dashboard tocar Control re-emitía Dashboard (modo atascado); ahora cada segmento emite su propio valor (enum `VehicleTab`). Bug 2 — el número del gauge estaba centrado sobre el pivote de la aguja; bajado a `Align(0, 0.55)`. Bug 3 — las ventanillas saltaban a cada poll (3 s); ahora interpolan en vivo con un `Ticker` (helper compartido `WindowPositionLerp`, 0.5/s como el mock, resincroniza en cada poll sin drift). Pestaña **Coche** — diagrama cenital interactivo del Hilux (`CarDiagram` + `CarDiagramPainter`): puertas que se baten al abrir, vidrios que bajan en vivo, faros por tipo e intermitentes parpadeando; puertas y vidrios tappables. 5 tests nuevos (96 Flutter). Verificado el mock en vivo con curl (la ventanilla avanza 0→0.51→1.0 a 0.5/s exactos).

### Fase 4 — Pulido y producto
- ~~**Media (archivos locales, metadatos)**~~ ✅ 2026-09-04, pulido 2026-09-05 — hecho: modelo `Track` (con `relPath`) + `MediaLibraryService` (escaneo incremental ffprobe, `MEDIA_DIR`) + `/media/tracks|library/scan|stream/:id (Range)|tracks/:id/play` + pantalla Flutter `/media` (biblioteca con búsqueda, reescaneo, **árbol de carpetas lateral**, **cola** con prev/next/auto-advance y **shuffle**). Reproduce vía el `AudioPlayerService` compartido con radio. La barra superior muestra `Media — <canción>` (y `Radio — <emisora>`); los títulos in-screen de Media y Radio se quitaron (solo barra superior). **Portadas/album art** ✅: `GET /media/tracks/:id/art` — cover de carpeta (cover.jpg…) o arte embebido extraído con ffmpeg al vuelo y cacheado en `MEDIA_DIR/.hiluxos-art` (marcador `.noart` de 7 días para no relanzar ffmpeg); la UI lo muestra como protagonista en now-playing y como miniatura en las filas. **Paneles plegables** ✅: chevrons en la fila de controles pliegan el raíl de carpetas y la biblioteca (patrón AnimatedContainer de Radio). **Selector de vista now-playing** ✅: popup junto a los controles con "Álbum" (portada, por defecto) o cualquiera de los 15 estilos del visualizador de espectro + "Aleatorio"; el pipeline de espectro del backend es genérico por URL, así que analiza `GET /media/stream/:id` sin cambios en backend (start/stop perezoso desde la pantalla: el ffmpeg solo corre con el modo espectro activo; listenerId por sesión evita pisar el de Radio). Fix: resume tras completar pista re-carga la fuente (el play grande ya responde). **Carpetas configurables** ✅ 2026-09-05: tabla `media_folders` (migración `20260905171213_add_media_folders`), seed con `MEDIA_DIR` **one-shot** (flag `Setting` `media.folders.seeded`; fix 2026-09-05: antes re-sembraba en cada listado con tabla vacía — la carpeta demo borrada reaparecía y el re-scan traía sus tracks de vuelta); `GET /media/folders` (con flag `exists` en vivo), `POST /media/folders {path}` (valida absoluta, normaliza, 409 duplicado), `DELETE /media/folders/:id` (purga tracks del índice; archivos intactos); el scanner itera todas las carpetas (`scanRoots()`, tabla vacía → scan no-op sin fallback a `MEDIA_DIR`); `relPath` se resuelve contra el prefijo de carpeta más largo. UI: **＋** en el raíl "Carpetas" abre el **selector de carpetas del sistema** (navegación real por el filesystem, `/browse`), tiles de raíz con ⚠️ "no disponible" para rutas desaparecidas (tap → SnackBar explicativo) y 🗑️ con confirmación; 409 al re-añadir una carpeta existente → mensaje "Esa carpeta ya está en la biblioteca". Vídeo aparcado — análisis y spike de arranque en `docs/ROADMAP-MEDIA.md`.
- ~~Pulido UI de **Radio** (shimmer en búsqueda, entrada animada de ítems, "pop" de favorito, now-playing vistoso), **System**, **Settings** (cabeceras, feedback).~~ ✅ 2026-09-05 — `Shimmer`/`ShimmerList` (core, pure-Flutter) en la búsqueda de Radio; ítems con `StaggeredEntrance`; corazón de favorito con pop elástico (`easeOutBack`); **now-playing strip** con favicon, metadatos y badge "EN DIRECTO" pulsante; emisora actual resaltada en la lista (tinte + borde primary). `SectionHeader` compartido (icono + título + divisoria) en las 3 cards de System ("Identity", "Resources", "Registro de eventos") y cabecera "Ajustes generales" en Settings; "Retry" → "Reintentar". Card **Sistema** del Home: celda vacía → **Versión** con hint `0.1.0 → 0.2.0` en verde cuando hay actualización OTA disponible (`updateProvider`). 10 tests nuevos (radio_polish 6, system_polish 2, system_widget 2).
- ~~**CI/CD** (GitHub Actions): build backend + `tsc`, `flutter analyze`, tests.~~ ✅ 2026-09-05 — `.github/workflows/backend.yml` (Node 22, `npm ci` con cache, `prisma generate`, `nest build`, **ESLint**, ffmpeg del sistema — el e2e de arte embebido lo necesita — y los 236 tests Jest/Supertest — sin BD: Prisma mockeado) y `.github/workflows/flutter.yml` (stable, cache de pub, `flutter analyze` + 91 tests). Disparan en push a master/dev/feature/develop y en PR (filtrados por `paths:` backend/** y app/**). **Primera corrida verde verificada en GitHub**: Backend 1m12s, Flutter 1m54s. Deuda previa resuelta: ESLint no estaba instalado ni configurado (el script `lint` existía pero el binario faltaba y usaba `--fix`); añadidos eslint@10 + typescript-eslint + config flat `backend/eslint.config.js` (reglas flojas para no reformatear estilo heredado), script `lint` sin `--fix` + `lint:fix`. La primera pasada encontró 2 bugs reales, corregidos: initializer inútil en `MediaFoldersService.mapFolder` y condición redundante `celsius !== null` en `SystemMonitorService.checkTemperature`.
- Proxy YouTube/Invidious (`POST /youtube/resolve`).

---

Cómo arrancar, pantallas y referencia API completa: `docs/USAGE.md`.
Arquitectura fuente de verdad: `ARCHITECTURE.md` (técnica) y
`docs/ARCHITECTURE-FUNCTIONAL.md` (funcional).
